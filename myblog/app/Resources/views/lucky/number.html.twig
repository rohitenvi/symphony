{% extends 'base.html.twig' %}

{% block body %}
    <h1>Your lucky number is {{ number }}</h1>
    <a href="{{ path('homepage') }}">Home</a>
     <a href="{{ path('blog_show', {'slug': 'rohit'}) }}"> k</a>
    
{% endblock %}
